# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Security

- **文档库内文件操作补上「不得越出库根」的统一校验**：此前只有「用外部工具打开」和「插入图片」校验相对路径，
  删除 / 重命名 / 移动、Office 预览、系统打开、批注、交付来源、AI 上下文等命令是把前端传来的相对路径直接拼到
  库根后面——遇到绝对路径（`C:\...`）会整个替换掉库根、遇到 `..` 会越出库根。现新增 `pathguard.rs`，所有
  「库根 + 相对路径」拼接统一拒绝绝对路径 / 盘符 / UNC / 反斜杠 / `..` / 控制字符，并带回归测试（越界的
  删除、重命名、移动不会触及库外文件）。
- **界面启用严格内容安全策略（CSP）**：此前 `csp` 为空即不设防。现限定脚本只能来自应用自身（仅为 PDF.js 的
  WebAssembly 放行 `wasm-unsafe-eval`）、禁止内嵌框架与插件对象、禁止表单提交，图片 / 字体 / Worker 仅放行应用
  自身、`blob:` 与 `data:`（Markdown 外链图片仍可显示）。

### Fixed

- **偶发闪退隐患**：「自写入标记」模块漏改了一处 `.lock().unwrap()`（写成了两行，此前批量替换没搜到），
  发布版遇到锁中毒会直接退出；现与其余 85 处一致使用 `lock_safe()`。
- **交付中心来源文档最多只能选 500 个**：现读取上限提高到 20000，并在来源列表上方增加路径筛选框与「已选 N 个」
  计数，大库也能完整挑选。
- **1440px 宽度下打开多个文档时标签栏偏窄**：有标签时搜索框收成图标（宽度 ≥ 1536px 时仍显示完整搜索框），
  把宽度让给标签栏。

### Changed

- **数据库读写分离**（原 GitHub Issue #1「连接池」待办）：此前所有命令共用一条数据库连接，扫描、保存等写事务持锁
  期间，打开文件、切库、搜索、列表等纯读取命令都要排队。现改为「一条写连接 + 4 条只读连接池」：写入与事务仍走
  写连接；纯读取走只读连接（`query_only`，从根本上杜绝误写），在 WAL 模式下不再被写事务阻塞。读取看到的是最近
  一次已提交的快照，写命令返回前已提交，随后发起的读一定能看到。
- 清除全部 8 个历史 clippy 警告（`cargo clippy --all-targets` 零警告）。

### Testing

- 前端新增组件级测试（jsdom + Testing Library，仅开发依赖，不进入发布产物）：`ErrorBoundary`（出错提示 / 技术详情
  折叠 / 重试恢复）、`FileTypeIcon`（系统图标回退 / 请求合并 / 失败容错）；新增 `format` / `prefs` / `sysIcons` 纯逻辑测试。
  前端测试由 3 个文件 17 项增至 8 个文件 36 项；Rust 测试 86 → 91 项。
- 后端新增：路径越界校验、只读连接池不被长写事务阻塞、只读连接拒绝写入。


## [0.11.0] - 2026-09-22

### Fixed

- **打开的文档标签较多时，超出标题栏宽度的标签无法切换到**：标签栏可横向滚动但隐藏了滚动条，普通鼠标滚轮也不会
  横向滚动。现在标签超出时两端出现左右箭头（滚到头时对应箭头置灰），鼠标滚轮在标签栏上横向滚动，激活的标签
  （包括新打开的文档）自动滚到可见位置；标签不多时不显示箭头。
- **搜索页单击结果无法打开文件**：单击结果此前只是跳到文档库并定位，不会打开文件（真实鼠标点击时选中项还可能
  错落到该文件夹的第一项）。现在单击结果直接按格式打开（编辑器 / PDF / Office / 图片查看器，无内置查看器的交给
  系统应用，与文档库双击一致）；原来的定位功能保留为每条结果右侧的「在文档库中定位」按钮（悬停显示）。
- **搜索页切到其他页面再回来，搜索词与结果被清空**：现在按文档库保留搜索词、结果与滚动位置，回到搜索页时原样
  恢复，并用同一关键词在后台静默刷新一次结果，保证与期间的文件增删改一致。
- **Word / Excel / PowerPoint 正文此前实际没有进入全文索引**：文档与说明一直写着「Office 正文已接入搜索」，
  但扫描只对文本类格式提取正文，Office 文件按内容永远搜不到（实测一个全是 Word 文档的项目库里搜「域控制器」，
  Word 命中数为 0）。现在扫描时一并提取 Office 正文（单文件正文上限 2MB，超出部分截断）；已有文档库会在下次
  扫描时自动补建（按「是否已有提取记录」判断，无需手动重建），之后未变化的文件不再重复提取。修复后同一搜索
  命中 Word 23 个、PowerPoint 5 个、Excel 4 个。
- **HTML / CSS / SCSS / LESS 等 20 种扩展名被归为「其他」**：编辑器早已支持它们的语法高亮，但格式注册表未登记，
  导致既不能在应用内编辑、也不进全文索引；现归入「代码」（`.csproj` / `.config` / `.xsd` / `.xslt` 等归入 XML）。
  已有文档库在下次扫描时自动更正格式并补建索引。
- **Office 打开文档时生成的 `~$` 锁文件出现在文档库和「最近打开」里**，且每次在 Word / Excel 中打开、关闭
  文档都会触发整库重扫：现在与 `~*.tmp`、`Thumbs.db`、`desktop.ini` 等系统临时文件一起在扫描和文件监听中排除。
- 扫描进度停在某个整数（如「已处理 1280 项」）像卡住：遍历过程按每 256 项上报，最后的零头与提取阶段此前都不上报；
  现在遍历结束上报准确总数，提取阶段也持续上报进度。

### Added

- **错误边界**：每个文档标签、主视图区与应用最外层各自兜底——某个视图渲染出错时只在该区域显示「暂时无法显示」
  与「重试 / 重新加载窗口」（可展开技术详情），其他标签与导航照常可用，不再整窗白屏。

### Changed

- **界面布局优化（在最小窗口 1024×640 与常用 1440×900 下逐页检查）**：
  - **开始页「最近文档库 / 最近打开的文件」列表被截断**（11 个库只看到 4 个半、最后一项被切一半）：列表带圆角
    裁切样式，在固定高度的页面里会被压缩并直接切掉内容。保持原单栏布局，文档库固定显示最近 5 个、文件显示最近
    4 个，常见窗口高度下首屏完整显示；超出部分通过标题行的「查看全部」进入「管理文档库」/「历史」；窗口很矮时
    整页滚动而不是裁切。
  - **小窗口下标题栏的最大化、关闭按钮被挤出窗口**，「文件 / 编辑 / 查看 / 帮助」被挤成两行竖排：搜索框改为可收缩，
    窗口按钮与菜单不再被压缩；窗口较窄时隐藏版本号与快捷键提示。
  - **小窗口下文档库中栏看不到文件名**（名称列被压到 0 宽，只剩图标）：文件列表改为固定表格布局，名称列占用其余全部
    宽度；窗口宽度小于 1280 时隐藏「类型」列（图标与右侧详情已表明类型），两侧面板相应收窄。
  - **编辑器、Office / PDF / 图片预览、交付中心的工具栏与状态栏文字折成两行**（「保/存」「转换为/Markdown」
    「UTF-/8」等）：统一不折行，空间不足时只截断文件路径；状态栏在窄窗口下隐藏说明性文字。
  - 「历史」页最近文档列表同样会被截断，改为整页滚动；「任务」页列表按内容高度显示，不再只有几条任务也留出
    一大块空白；列表类页面的宽度与边距统一。
  - 标题栏文档标签、开始页文档库也改用系统文件 / 文件夹图标，与文件列表一致；「最近打开」不再显示 Office 锁文件；
    搜索无结果时的提示不再错误地说「短关键词匹配范围有限」。
- **文件图标改为按扩展名取本机标准图标，并改为进程内提取**：此前按格式分类取图，所有代码文件（`.ts` / `.cs` /
  `.ps1`…）都显示成同一个图标、`.jpg` 显示 `.png` 的图标；现在按扩展名取，与资源管理器完全一致，文件夹也使用
  系统文件夹图标。提取方式由启动 PowerShell 子进程（每次启动约 1–2 秒、需创建临时探测文件）改为进程内 Win32 调用
  （约 0.06 秒、不访问磁盘），取系统 48px 超大图标，高分屏下更清晰；图标资源不随应用分发，全部来自用户本机。
- **启动更快**：编辑器（Tiptap / CodeMirror）、PDF.js、Office 查看器、交付、设置等较重模块改为按需加载，
  首屏主包由 1,981 KB 降至 361 KB（gzip 617 KB → 108 KB）。
- **首次建立 / 补建全文索引时不再长时间卡界面**：写库拆为「元数据单事务 + 正文分批写入」，每批单独短暂持锁；
  以 1,116 个文件（含 572 个 Office 文件、约 14MB 正文）的真实文档库实测，最长一次持锁由 4,770ms 降至 141ms，
  期间浏览目录、搜索不再被阻塞。
- **同一文档库的并发扫描自动合并**：扫描进行中再来的请求不再并行重复扫描，而是结束后补跑一轮；切换到已在文件
  监听中、且 10 分钟内完成过全量扫描的文档库时不再重扫（变化已由监听实时同步），大文档库来回切换不再反复遍历。
- **互斥锁中毒可恢复**：此前任一命令在持有数据库锁时 panic，之后所有数据库命令都会失败直到重启；现在取回连接
  继续使用（未提交的事务已在 panic 时回滚），全部 85 处加锁统一处理。
- 详情面板预览：选择停留 180ms 后才读取（快速点击 / 浏览时不为每个经过的文件读盘）；超过 20MB 的图片、50MB 的
  PDF 与 512KB 的文本不做预览；没有预览的文件（文件夹、Office 等）不再显示空的「预览」标题。
- 界面去掉未实现功能的禁用占位：侧栏「大纲 / 引用」标签、并列布局中 4 个「未开放」的智能集合、详情面板的
  「标签 / 相关文档」占位框；建库向导中三个只记录设置、不产生任何效果的开关改为如实的能力说明（全文索引自动开启，
  图片文字识别在图片查看器中按需进行）。
- 文案如实化：搜索页说明（Office 正文可搜、PDF 正文暂不支持）、任务中心页脚、关系图页徽标不再出现「按路线图交付」
  等开发用语；「开始」「历史」页最近文件列表改用系统文件图标。
- 《使用指南》《开发指南》同步更新：索引能力、搜索规则（更正了「短词只匹配文件名」的错误描述：短词同样匹配正文，
  只是较慢）、系统临时文件过滤、错误边界、扫描两段式写库与合并机制、模块职责与测试清单（86 项）。
- 《使用指南》《开发指南》更新至 v1.6：搜索（单击打开、定位按钮、状态保留）、开始页「查看全部」、文档标签滚动、布局与窗口尺寸适配等说明同步。

## [0.10.0] - 2026-09-22

### Added

- **详情面板文档预览**：右侧详情面板底部新增「预览」区——图片显示图片本体、PDF 渲染首页、文本类
  （Markdown / 代码 / JSON / YAML / XML / 配置 / CSV）显示前 12 行摘录；Office 等其余格式不显示，
  读取失败时静默跳过、不影响面板其余信息。新增「设置 → 外观 → 详情面板预览」开关（默认开启），
  关注性能或低配设备可关闭。

### Changed

- 右键菜单「删除（进回收站）」简化为「删除」，行为不变（仍是移入系统回收站，可还原）。

## [0.9.1] - 2026-09-22

### Fixed

- **中央文件列表此前没有右键菜单**：「复制路径」及整个右键菜单（重命名 / 移动 / 删除等）此前只接在
  侧栏目录树上，中栏文件列表行右键无响应；现已接上同一菜单，两侧行为一致。

### Changed

- **旧版 .xls / OpenDocument .ods 打开时自动改用版式预览**：装有 Microsoft Office / LibreOffice 时
  双击这类文件会自动在后台生成版式预览并直接显示（与 Word 的行为一致），不再停留在文字提示；
  生成期间显示专用加载占位，不再闪现「旧版 .xls」提示，版式预览失败后才回退到可读提示。
  .ods 此前还会报「缺少 xl/workbook.xml」内部错误，已改为可读提示并补充回归测试。

## [0.9.0] - 2026-09-22

### Added

- 侧栏目录树与中央文件列表的右键菜单新增「复制路径」：复制该文件 / 文件夹的 Windows 绝对路径，
  复制成功后短暂提示。

### Fixed

- **旧版 .xls 文件打不开时报内部结构错误**：旧版二进制 .xls（以及已加密的 Office 文件）是 OLE2
  复合文档而非 zip 包，此前会被当作 .xlsx 解析并报「缺少 xl/workbook.xml」。现在按文件魔数提前
  识别，提示改用版式预览（需 LibreOffice / Office）、系统应用打开或另存为 .xlsx。

### Changed

- 界面文案统一：「创建文档库 / 新建文档库」统一为「添加文档库」——各入口（开始页、文档库页、
  管理文档库、标题栏下拉、文件菜单）打开的是同一个向导，且都只索引已存在的文件夹，并无「新建」
  语义；向导标题、步骤名「确认创建 → 确认添加」与相关提示同步调整，文档相应更新。
- 《使用指南》《开发指南》同步：复制路径菜单项、旧版 .xls 说明、测试清单（74 项）。

## [0.8.2] - 2026-09-22

### Fixed

- **「套用表格格式」工作表打开后看不出表格线**：这类工作表通常关闭了普通网格线，行列分隔原本由 Excel
  内置表格样式（如 TableStyleMedium2）按样式名动态绘制，而样式定义并不保存在文件里，应用也没有实现
  这套绘制，于是表格区域完全看不出行列分隔（同一文件里手工加过边框的工作表则正常）。现在表格数据
  范围内的单元格始终绘制一圈浅色网格线，保证行列可辨；手工边框仍按原样式显示。

### Changed

- 《使用指南》《开发指南》更新至 v1.4：补充「套用表格格式」筛选识别与表格线显示的说明，测试清单同步。

## [0.8.1] - 2026-09-22

### Fixed

- **Excel「套用表格格式」（插入表格）工作簿打开后筛选按钮形同消失**：这类工作簿的筛选范围记在
  `xl/tables/tableN.xml` 表格定义里，而不是工作表本身的 `<autoFilter>` 节点上，此前只读取后者，
  导致这类很常见的报表打开后筛选功能一直处于「未检测到」状态。现在工作表本身没有筛选范围时会回退
  读取其引用的表格定义；用真实用户文件（5 个工作表均用表格）验证过修复前后差异。
- **Excel 原生表格视图解析失败时显示原始 zip 报错**（如 `OOXML 包打开失败: invalid Zip archive:
  Could not find EOCD`，常见于误把旧版 .xls / 已损坏文件当作 .xlsx 打开）：改用 Word / PowerPoint
  预览早已接入的 `friendlyOfficeError()` 统一转换为可读的中文提示，不再直接展示底层报错。

## [0.8.0] - 2026-09-22

### Added

- **单个文件夹显示上限可调**（设置 → 外观）：目录树与中央列表一次渲染的条目数默认 2,000（可调整具体数值），
  也可选择「不限」；改动立即对所有已打开的库生效。
- **超大文件夹保护**：目录树与中央文件列表都不做虚拟滚动，单个文件夹几千上万个条目会直接生成对应数量的
  DOM 节点，导致界面明显卡顿甚至短暂无响应。现在单个文件夹一次最多渲染 2,000 项，超出部分给出「还有 N 项
  未显示，请用搜索定位」提示；实测 5,001 个文件的文件夹，目录树与中央列表均正常显示且无卡顿。
- **超大文件保护**：PDF / 图片查看（含编辑器内嵌图片）此前读取文件时没有大小上限，误打开体积极大的文件
  （如改了扩展名的视频、镜像文件）可能长时间无响应甚至耗尽内存。现在超过 300MB 会直接提示「文件过大…请
  使用系统应用打开」而不再尝试读入内存；实测 320MB 文件按预期被拒绝并显示正确的大小与上限。
- 图片查看器的错误提示补上「使用系统应用打开」按钮（此前只有 PDF 查看器和编辑器有这个后备入口）。

### Changed

- 「转换为可编辑文档」改名为「转换为 Markdown」（按钮、对话框标题、相关提示统一），明确转换的目标格式；
  转换确认框新增提示：Markdown 是纯文本格式，文字颜色 / 字体 / 字号 / 对齐方式 / 页面版式等视觉格式不会保留
  （标题、加粗斜体、表格、链接、图片会保留），装有 Office / LibreOffice 时提示可改用版式预览按原样查看。
- 编辑器「无法编辑」提示、「关系图」占位页、任务中心说明里过时的 `v0.1`–`v0.3` 开发阶段版本号（这些标记不该
  出现在用户看到的文案里）改为准确、与当前实际能力一致的说明。
- AI 敏感信息放行两处按钮「知情并发送 / 知情并继续发送」改为「仍要发送」，与全应用统一使用的
  「仍然保存 / 仍然切换」等强调型按钮措辞一致。
- 《使用指南》《开发指南》更新至 v1.3：补充单个文件夹显示上限设置、修正已改名的「仍要发送」按钮文案、
  README 版本号与路线图同步至 v0.8.0。
- 窗口内图标（自绘标题栏、开始页、浏览器 favicon）此前是蓝色 M 标志，与安装包 / 任务栏程序图标（太极风格）
  是两套不相关设计；现在统一使用程序图标同款设计。

### Fixed

- 调用 Pandoc / LibreOffice 等外部组件失败时，错误提示不再直接显示 Rust 的 `Option` 调试格式
  （如 `Some(64)`），退出代码正常显示为数字；用真实损坏文件验证过修复前后的提示文本差异。

## [0.7.1] - 2026-09-22

### Fixed

- **文档库大到一定规模后，外部删除子目录再重新打开会长时间无响应（严重）**：全文索引表 `search_fts` 早期把 `file_id` 存成未索引列，按它做的删除（保存、重扫、外部文件被删除后清理索引都会触发）等价于全表扫描；索引积累到几十万条正文记录后，一次删除几百个文件（如外部删掉一个含很多文件的子目录后重新打开库）可能卡住数十秒甚至被系统判定为「未响应」而被关闭。现在 `file_id` 直接是 `search_fts` 的 `rowid`（天然索引，删除是索引查找），已有数据库首次打开时自动按 `extracted_content` 重建一次索引，无需手动处理；100,000 条记录中删除 500 个文件的实测耗时从预期数十秒降到约 90 毫秒。
- **单文件模式下「转换为可编辑文档」，一段时间后重新打开会提示「文件不存在」**：转换生成的 `.md` 副本此前只登记进了索引，没有登记进单文件模式的「白名单」，随后的后台重扫会把不在白名单里的文件当作「已消失」清理掉，副本明明还在磁盘上却打不开了。现在转换时会一并登记进白名单。
- 上述两类「文件不存在」的报错以及打开文档库失败的报错，不再直接拼接底层数据库错误原文（如 `Query returned no rows`），改为给出中文说明与建议操作。

### Changed

- SQLite 连接实际启用 WAL 日志模式（此前文档已写但代码未生效）：写入不再独占整个数据库文件，也不需要为每次保存重写回滚日志。
- 「设置 → 关于」加入用户图标；帮助菜单「关于 MarkFlow」现在打开设置页的「关于」分类（原先是内容早已过时的独立弹窗）。
- 「文件」菜单「关闭当前文档库」去掉多余的后缀说明，文案与其余菜单项保持一致。
- 侧边栏「管理文档库」的「完成」按钮改名为「返回」，移动到底部「添加文档库」按钮上方，不再放在标题栏右侧。

## [0.7.0] - 2026-09-22

### Added

- **源码模式 / 纯文本自动换行**（默认开启，「设置 → 编辑器」可关）：`.txt`、日志等长行不再需要左右拖动。
- **状态栏字数与光标位置**：编辑时显示字数（中日韩逐字、其余按词，Front Matter 不计）；源码模式再显示「第 N 行，第 M 列」。
- **可视化编辑器工具栏**：新增删除线、行内代码、分割线、插入图片；标题改为「段落样式」下拉（正文 / 标题 1–6）。
- **表格操作**：光标在表格内时出现操作条——上 / 下方加行、左 / 右侧加列、删除行 / 列、删除表格。
- **图片**：工具栏插入图片，或**直接粘贴截图**；图片保存到文档旁的 `assets/`，Markdown 里写相对路径，编辑器内即时显示（相对路径经索引读取，不依赖外部权限）。
- **Ctrl+K 在可视化编辑器内为插入 / 编辑链接**；全局搜索新增 `Ctrl+Shift+F`（任何位置可用），编辑器外仍可用 Ctrl+K。
- **代码文件编辑**：源码模式统一为一套 CodeMirror 6 配置（语言注册表 + 扩展工厂），新增 JavaScript / TypeScript / Python / HTML / CSS / SQL / PowerShell / C# / Shell / Dockerfile / INI / TOML / 批处理等语言的语法高亮；识别顺序为文件名（如 `Dockerfile`、`.gitignore`）→ 扩展名 → Shebang → 手动选择 → 纯文本降级，状态栏可直接点击切换语言。
- **状态栏代码信息**：语言、编码（点击可选择转换，默认保持原编码）、换行符（点击可选择转换，默认保持 CRLF/LF 原样）、缩进（按文件内容自动推断）、保存状态、只读 / 保护模式标记、诊断数量。
- **语法诊断**：JSON / JSONC（容忍注释与尾逗号）/ YAML / XML 语法校验，精确到行列并在编辑器里加波浪线；新增「问题」侧栏面板，点击定位到出错位置。
- **只读文件保护**：磁盘只读属性的文件以只读模式打开（顶部提示 + 无法保存），避免保存时报错才发现。
- **大文件保护**：超过阈值（默认 2MB，可在「设置 → 编辑器」调整）的文件自动关闭语法高亮 / 折叠 / 诊断以保证流畅，仍可正常编辑与保存；超过应用内编辑上限（默认 50MB）拒绝打开并引导使用外部工具。
- **保存时可选转换编码 / 换行符**：默认永远按磁盘原样写回；仅当用户在状态栏显式选择「转换为 GBK / CRLF」等才会在下次保存时生效，目标编码无法表示的字符会中止保存并提示，不会损坏文件。
- **外部工具入口**（文档工具栏「打开方式」）：用 VS Code 打开文件 / 所在目录、系统默认程序、在 PowerShell / CMD 中打开所在目录、在资源管理器中显示；路径以参数数组传递，中文与空格路径安全；未安装 VS Code 时对应项禁用并提示。

### Changed

- 「设置 → 关于」开发者一行加用户图标，版式更贴近常见桌面应用「关于」页。

### Fixed

- 同一个列表里既有 `- [ ]` 任务项又有普通项时，可视化模式会拆成两个列表并多出一个空任务项：这类文档现在默认以源码模式打开并提示风险。

## [0.6.1] - 2026-09-22

### Fixed

- **Word 转为可编辑文档后打不开**：转换完成后立即打开的 Markdown 副本此时还没被后台重扫收录进索引，读取时报「文件不存在于文档库索引」。现在转换命令在返回前先把副本同步登记进索引（并有回归测试），附件等其余文件仍由随后的重扫收录。
- 文档标签里发起的「打开」（转换出的副本、Office 版式预览切换等）现在始终针对标签所属的文档库，而不是当前活动库——多库并存时，活动库与标签所属库不同会导致「文件找不到」。

## [0.6.0] - 2026-09-22

### Added

- **开源发布**：仓库转为 Public，许可证 GPL-3.0-only（新增 `LICENSE`、`THIRD-PARTY-NOTICES.md`、`SECURITY.md`）；「设置 → 关于」展示版本 / 开发者 / 仓库 / 许可证与隐私说明。
- **多文档库**：
  - **文档库选择器（默认）**：左侧顶部下拉选择文档库（按名称排序），下面只显示当前库的「文件」目录树，底部有「添加文档库」「管理文档库」；可在「设置 → 外观 → 文档库列表」切换为并列布局。
  - **并列多库**（可选布局）：所有已打开的库按名称排序并列，每个库可折叠 / 展开，折叠状态持久化，**默认全部折叠**；库名行悬停可就地新建文档 / 文件夹、从工作区关闭该库（不删除文件）。已打开的库集合与上次的活动库在重启后恢复；文件监听对每个已打开的库同时生效。
  - **管理文档库**（侧边栏管理模式，不弹窗）：列出全部文档库，可「打开」「关闭」（仅从当前窗口拿掉，仍在列表中）或「移除」（删除索引记录，不删原文件）。移除后可用「添加文档库」重新选择该文件夹再次打开，且**历史版本、批注、交付记录会恢复**（重新添加同一文件夹沿用原库 ID）。
  - **库名标识**：建库时名称必填且不可与已有库重名（默认取文件夹名，重名自动加序号）；新建文档 / 文件夹、重命名、移动、删除对话框显示所属库名；Ctrl+N 在有多个库时先选目标库；AI「存为文档」提示保存到哪个库。
  - **默认定位**：打开文档库自动定位到第一项（目录优先），左侧树自动展开、中央列表显示其内容。
- **文档多开（标签页）**：打开的 Markdown / PDF / Office / 图片 / 交付中心保留为标题栏里的标签页，切换视图或点「返回」不会丢失，只有点标签上的 × 或 Ctrl+W 才关闭；每个标签独立保留未保存的编辑与滚动位置，未保存的标签显示黄点，关闭时确认。库名默认隐藏（悬停提示），仅同名文件时淡色显示。
- **设置页重做**：左侧分类导航（通用 / 外观 / 编辑器 / 预览 / AI / 组件 / 关于）+ 右侧内容区，顶部可搜索设置；每项统一为「名称 + 说明 | 控件」，改动立即生效；不再展示未实现的「开发中」占位；AI Provider 的添加 / 编辑表单收进卡片。
- **外观：深色 / 浅色 / 跟随系统主题**（默认浅色）。深色主题通过重映射颜色变量实现，编辑器、设置页、各视图一致；Word / Excel / PPT / PDF 的文档页面始终保持白纸底色。
- **开机启动**（设置 → 通用）：写入当前用户的 `Run` 注册表项，不需要管理员权限；开机自动拉起时在后台启动（开启「最小化到通知区域」则驻留通知区域，否则最小化到任务栏）。
- **关闭窗口时最小化到通知区域**（设置 → 通用）：开启后关闭只隐藏窗口、应用继续在后台运行，此时未保存的编辑仍在内存中；点击通知区域图标或再次启动 MarkFlow 恢复窗口，图标菜单「退出」或「文件 → 退出」才会真正退出（有未保存修改时先确认）。
- 状态栏左侧显示当前文档库名称（悬停显示完整路径）。
- **Excel 表格支持筛选**：工具栏「筛选」开启后，表头行每列出现筛选按钮，点开是值列表（含搜索、全选 / 清除、各值出现次数），按值筛选并隐藏不匹配的行（行号保持原样），显示「X / Y 行」，可一键清除全部筛选。表头行自动识别（优先文件里的自动筛选范围，其次冻结窗格的最后一行），也可手动选择；文件本身带自动筛选时默认开启。筛选只影响显示，不修改文件。
- Word / PowerPoint 预览工具栏新增「内置渲染 | Office 版式」切换（装有 Office / LibreOffice 时显示）：一键在内置渲染与精确版式之间切换当前这份文档，不必进设置页；Office 版式查看器里的同名控件可切回内置渲染（切回后本次不再自动调用 Office）。全局默认仍在设置中选择。

### Changed

- **Word / PowerPoint 预览引擎默认改为「内置渲染」**：即时显示、左侧目录导航（按 Word 导航窗格同样的依据——标题样式与大纲级别提取）、不再默认启动 Microsoft Office（也不再在选中文件时后台预热）。想使用 Office 导出的精确版式：在「设置 → 预览」选「自动（有 Office 优先）」，或在文档工具栏点「版式预览」手动调用。已经在设置里明确选过「自动」的用户保持不变。
- 「设置 → 关于」改为平铺版式：产品名与简介、版本 / 开发者 / 源码仓库 / 许可证、第三方组件说明、「隐私与数据」说明（不收集遥测；唯一对外请求来自用户配置的 AI Provider）。
- 标题栏合并为单一的文档标签行（原「当前文档」小标签移除），标签行空白处可拖动窗口、双击最大化 / 还原。
- 指南升级为 v1.2（`docs/2026-09-22-…-v1.2.md`），README、AGENTS.md、开发指南补充许可与合规、公开仓库检查清单。

### Fixed

- 文本编辑器（.txt 等）内容较长时没有滚动条。

## [0.5.0] - 2026-09-21

### Added

- **无 Office / LibreOffice 环境的内置渲染**（虚拟机、未装 Office 的机器）：
  - Word 用 `docx-preview`（Apache-2.0）渲染为带分页的页面：标题样式、加粗 / 斜体 / 颜色、表格与底纹、图片、页眉页脚、脚注；
    左侧目录可点击跳转。装了 Office 时也用它在后台生成精确版式期间先行显示。
  - PowerPoint 用 `@aiden0z/pptx-renderer`（Apache-2.0）渲染为连续滚动的幻灯片（窗口化挂载）+ 缩略图导航：
    文字、形状、图片、表格、常见图表；不含动画 / 切换，SmartArt 与 3D 只是近似。
  - 两个渲染器都按需动态加载（不影响启动）；文档里的超链接不会在应用窗口内导航，只允许 http / https / mailto 且需确认后交给系统浏览器；
    渲染失败自动回退到文本预览并说明原因；旧版 .doc / .xls / .ppt 与加密文件给出明确提示。
  - 环境变量 `MARKFLOW_NO_OFFICE=1` 可禁用 Office / LibreOffice 版式引擎（用于测试或排查外部引擎问题）。
- Word / PowerPoint 工具栏新增「纯文本 / 文档视图」「文本大纲 / 幻灯片」切换。
- **设置项「Word / PowerPoint 预览引擎」**：默认「自动（有 Office 优先）」；选择「始终使用内置渲染」后，
  即使装了 Microsoft Office / LibreOffice 也不启动它们（同时停止选中文件时的后台预热），打开更快、不依赖外部软件；
  工具栏的「版式预览」按钮仍可随时手动调用 Office 导出精确版式。Excel 始终使用原生表格。

### Changed

- **Office 阅读体验重做，按格式选择最合适的默认方式：**
  - **Word：** 打开先显示目录（由标题样式 / 大纲级别提取）与文字，同时后台用 Microsoft Office 生成精确版式，
    完成后自动切换为连续阅读；可随时取消；选中文件时后台预热缓存，二次打开秒开。
  - **Excel：** 默认使用原生表格视图，不再转 PDF——列宽 / 行高、合并单元格、冻结窗格、字体 / 颜色 / 填充 / 边框 / 对齐、
    日期 / 百分比 / 千分位 / 货币等数字格式与 Excel 一致，底部为工作表标签；另提供单独的「打印版式预览」（导出前按页宽缩放）。
  - **PowerPoint：** 逐页图片查看器（缩略图导航 + 单页大图），导出一页显示一页，方向键翻页；
    只装了 LibreOffice 时回退为 PDF 版式预览，都没有时为文本大纲。
- **PDF 查看器重写为连续滚动：** 页面虚拟化（只渲染视口附近，远离即释放，长文档不占内存）、可选中并复制文字、
  搜索结果在页面上高亮、缩略图导航、页码输入框、自适应宽度 + 缩放。库内 PDF 与 Office 版式预览共用。
- 外部程序（Word / Excel / PowerPoint / PDF 工具）保存文件后回到 MarkFlow 窗口，预览自动刷新。
- Office 导出串行执行（PDF 与幻灯片共用一把锁），避免多个 Office 实例互相抢占。

### Fixed

- 用户在版式预览生成期间离开后，结果返回时仍会重新弹出查看器。

## [0.4.2] - 2026-09-21

### Changed

- Office 文档（DOCX / XLSX / PPTX 及 DOC / XLS / PPT / ODT / RTF）默认显示**版式预览**：
  本机装有 Microsoft Office 时，后台只读、禁用宏、无界面地导出 PDF 再显示，
  版式与 Office 中一致（此前只有装了 LibreOffice 才有，且仅显示提取文字，格式全部丢失）；
  结果按「路径 + 修改时间 + 大小」缓存（上限 60 个 / 400 MB），文件变化后自动失效。
  回退顺序：Microsoft Office → LibreOffice → 文本快速预览；失败时顶部说明原因。
- 版式预览可随时切换到「文本预览 / 转换」；「版式预览」按钮不再依赖 LibreOffice。
- Office → PDF 转换改为异步执行，不再阻塞界面。

## [0.4.1] - 2026-09-21

### Added

- 标题栏左上角显示应用版本号（原在状态栏右下角）。
- 标题栏显示当前打开的文档名并提供关闭按钮；新增「文件 → 关闭文档」与 `Ctrl+W`。
- 「历史」视图新增「最近打开的文档」（点击直接打开，不依赖文档库）；
  库内文档通过界面打开时也会记入最近打开。

### Fixed

- 打开 Office 文档（以及设置页、交付页）时会自动弹出 Edge 浏览器并抢占前台：
  组件检测曾执行 `msedge.exe --version`，而 Windows 上 Edge 不会打印版本而是直接启动浏览器窗口。
  现在改为读取安装目录的版本号文件夹，不再执行 Edge；Office 预览只做轻量的 LibreOffice 存在性检查。
- 调用 Pandoc / LibreOffice 等外部命令时不再闪现控制台黑窗（`CREATE_NO_WINDOW`）。
- 「历史」视图在没有保存过文件时整页为空，缺少说明。

## [0.4.0] - 2026-09-21

### Added

- 菜单栏（文件 / 编辑 / 查看 / 帮助）与快捷键：`Ctrl+O` 打开文件、`Ctrl+N` 新建文档、
  `Ctrl+/-/0` 与 `Ctrl+滚轮` 缩放、`F11` 全屏。
- 打开任意文件：菜单 / 开始页 / 拖入窗口 / 命令行参数；库内文件定位到所属文档库，
  库外文件进入「单文件模式」（隐式库，仅索引已打开文件）；最近打开的文件列表。
- 单实例：已运行时双击 `.md` 会把文件交给现有窗口。
- 安装包：NSIS 增加简体中文并按系统语言自动选择（中文系统不再显示英文向导）；
  注册 `.md` / `.markdown` 文件关联。
- 状态栏缩放百分比可展开预置档位（双击复位 100%）。
- 编辑器：自动保存开关（设置）、崩溃草稿恢复、窗口重新获得焦点时检测外部修改、
  编码 / 字符数 / 行数状态条、链接插入。
- AI：Provider 可编辑；HTTP 明文地址警告；备用 Provider 回退；生成可取消；
  发送前展示范围 / Token / 去向并确认；回答标注实际 Provider 与模型；【来源 n】可点击跳转。
- 应用内对话框（替代原生 alert / confirm / prompt）。

### Changed

- 文档库扫描改为增量更新：文件 id 稳定，遍历与正文提取阶段不再持有数据库锁，
  扫描期间列目录、搜索、保存不再被阻塞。
- 文件监听忽略应用自身写入与临时文件，保存不再触发整库重扫。
- AI 资料以带边界的 user 消息注入并声明为不可信数据，降低 Prompt 注入风险。
- 短关键词（不足 3 个字符）搜索现在同时匹配正文。
- 文件名校验补齐 Windows 规则（非法字符、保留设备名含 `con.txt` / `com1`、尾部点号与空白）。
- 智能集合 / 标签占位入口明确标注「未开放」。

### Fixed

- 非 UTF-8（如 GBK）文本文件保存后被静默损坏；现按原编码与原换行符写回，
  无法无损编码时拒绝保存。
- 切换文档库 / 关闭窗口 / 返回文档库 / 打开其他文件时，未保存修改被静默丢弃。
- 源码模式切回可视化模式丢失未保存编辑；AI 润色应用后可视化编辑器不更新。
- Front Matter 与可视化无法无损往返的语法在可视化编辑保存后被改写：
  现原样保护 Front Matter，其余语法默认进入源码模式并在切换前提示。
- 重命名 / 移动文件后历史快照与批注失联。
- 敏感信息扫描在手机号 / 身份证 / 密钥紧贴中文时漏报；命中现标注所属文件；用户输入同样扫描。
- AI 回答排版塌陷、中文输入法 Enter 误触发发送、SSE 分片导致中文乱码、
  流式更新函数内嵌套 setState。
- 删除文档库时遗留全文索引记录。
- 双击无内置查看器的格式没有任何反应（现交给系统默认应用）。
- 清理误提交的异常命名文件（失败的 `cp` 命令被当作目录名）。

## [0.3.1] - 2026-09-21

### Fixed

- 目录树展开体验修复：箭头改为独立按钮（单击即展开 / 收起，无需双击）；
  重扫、保存、外部文件变化后**不再折叠整棵树**，展开状态与当前目录保持；
  重新打开同一文档库时自动重扫一次，闭库期间的磁盘变化即时反映。

### Added

- 文档体系补充：重写 README（功能特性 / 安装方式 / 快速上手 / 项目结构 /
  架构概览 / 故障排查 / 文档索引）；新增《使用指南 v1.0》（分功能操作手册、
  数据存储与隐私说明、FAQ）与《开发指南 v1.0》（环境搭建、模块职责、
  事件契约、测试策略、Git 工作流、发布流程落地步骤、常见开发问题）。

## [0.3.0] - 2026-09-21

### Added

- 文档视图缩放（Word 状态栏样式）：状态栏右下角「− 滑块 百分比 +」，
  点击百分比复位 100%；作用于 Markdown 可视化/源码编辑器（50%–250%）、
  PDF 阅读（40%–400%）、图片查看（25%–500%）、Office 快速预览（50%–250%）；
  PDF 工具栏缩放按钮与状态栏共享同一缩放状态。
- 目录树文件操作（§6.3）：右键菜单支持新建文档 / 新建文件夹 / 重命名 /
  移动到… / 删除（进系统回收站可还原）；同级同名冲突、目录移动进自身、
  文件被占用等场景明确报错；名称合法性校验（路径分隔符 / 连续点号 /
  隐藏前缀 / Windows 保留名）；树头部与工具栏「新建文档 / 新建文件夹」接线。

### Fixed

- 目录树中无法打开文件——双击文件现在按格式路由（编辑器 / PDF 阅读器 /
  Office 预览 / 图片查看），与文件列表行为一致；
- 编辑器或查看器打开时，左侧活动栏（开始 / 搜索 / 任务等）点击无效——
  现在统一走视图切换入口，编辑器与查看器自动收起；
- 同类遮挡问题：标题栏切换文档库、删除当前库、搜索结果点击聚焦、
  交付中心打开等场景下编辑器 / 查看器状态残留，现已互斥关闭；
- 编辑器存在未保存修改时，切换视图弹出离开确认，防误丢修改。

## [0.2.0] - 2026-09-21

### Added

- Tauri 2 + React 19 + TypeScript + Vite + Tailwind CSS 4 工程骨架；
  应用外壳：无边框窗口、自定义标题栏（库切换）、活动栏导航、状态栏。
- 文档库核心：建库向导（三步）、目录树（懒加载）、文件列表、详情面板、
  格式注册表（17 类）、SQLite 索引库、目录扫描（排除规则、上限保护、后台事件）、
  标题栏库切换菜单与最近文档库。
- 文件监听与自动重索引：notify 递归监听、1.2 秒防抖全量重扫、
  被占用文件跳过不中断扫描。
- 全文检索与搜索视图：文本类格式正文提取 + FTS5 trigram（支持中文子串）、
  命中片段高亮、结果定位跳转、Ctrl+K。
- 后台任务中心：进度、取消、完成摘要、失败原因与历史清理。
- 原生编辑闭环：Markdown 可视化画布（Tiptap）+ 源码模式（CodeMirror 6）、
  原子保存、外部修改冲突检测（覆盖 / 重新载入 / 取消）、
  历史快照与一键恢复（每文件 20 个版本）。
- PDF 阅读与 Office 快速预览：PDF.js 渲染 / 翻页 / 缩放 / 跨页文本搜索；
  DOCX / XLSX / PPTX OOXML 安全提取（正文可检索）；系统应用打开。
- 转换与导入：组件管理器（Pandoc / LibreOffice / Edge 探测）、
  DOCX → Markdown 可编辑副本（转换预检：加密阻断、宏 / 修订 / 批注提示）、
  DOCX / HTML 导入、可选 LibreOffice 高保真预览。
- AI 工作台：OpenAI 兼容 Provider（API Key 存 Windows 凭据库）、
  连通性测试分类、上下文门禁与敏感信息扫描、流式引用问答、
  AI 润色行级差异审阅、结果存为文档、文档质量检查（标题层级 / 断链 /
  空章节 / 敏感信息）。
- Windows OCR 与批注：图片文字识别入全文索引、图片查看器、
  引用选中文本批注（解决 / 重开 / 删除 / 位置重定位检测）。
- 正式交付中心：质量门禁预检、来源 SHA-256 冻结、
  Markdown / HTML / DOCX / PDF / ZIP 五格式管线、产物验证与原子落盘、
  交付清单与交付历史。
- 仓库标准文件与产品设计文档（docs/产品设计/）；
  测试文档库基线（D:\MarkFlow，24+ 个多格式文件）。

### Verification

- Rust 单元 / 集成测试 31 项（格式识别、扫描排除、检索往返、任务生命周期、
  Office 提取、编辑冲突与历史、交付门禁管线等），零警告；
- 5 万混合文件库性能 Spike：扫描 + 提取 + 索引约 9,092 文件/秒，
  中文全文检索 30ms（命中 5,000），文件名检索 0.27ms。

## [0.1.0] - 2026-09-20

### Added

- 初始工程骨架：Tauri 2 + React 19 + TypeScript + Vite 工程、
  无边框窗口与自定义标题栏、活动栏一级导航、状态栏、设计令牌与浅色主题、
  仓库标准文件与产品设计文档。
